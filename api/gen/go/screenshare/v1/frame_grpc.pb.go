// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.0
// - protoc             v7.35.1
// source: screenshare/v1/frame.proto

package screensharev1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	FrameService_Frames_FullMethodName = "/screenshare.v1.FrameService/Frames"
)

// FrameServiceClient is the client API for FrameService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// FrameService is the second service on the control socket, carrying handles and not pixels.
//
// A decoded frame stays in the GPU memory the decoder allocated.
// What crosses here is the name of that memory, a shared texture handle on Windows or a dmabuf
// file descriptor on Linux, with the bookkeeping that says whose turn it is to touch it.
// Pixels never enter a message, never reach system memory and never reach ControlService.
//
// Nothing here describes a tile or a grid.
// A subscription names a picture some effect already opened, and the size it asks to be
// rendered at is a count of pixels rather than a layout
// (docs/ipc-api.md, docs/viewer-architecture.md).
type FrameServiceClient interface {
	// Frames subscribes one consumer to one decode.
	//
	// Bidirectional because the protocol is a loan: the backend lends a slot of shared memory
	// and writes into it again only once the consumer hands it back, so the release rides the
	// call the frame did.
	// A release on a second call could outlive the subscription it belongs to.
	//
	// One call per decode rather than one call for all of them, so a tile that ends cancels
	// its own subscription and a consumer that dies mid-frame is one dead stream rather than
	// every stream stalling on a slot nobody returns.
	//
	// FAILED_PRECONDITION where nothing is producing what the subscription named.
	// This service opens none of the three pictures: StartReceive opens a relay decode, the
	// publish opens its own preview, and StartMonitorPreview opens a monitor's.
	Frames(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[FramesRequest, FrameEvent], error)
}

type frameServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewFrameServiceClient(cc grpc.ClientConnInterface) FrameServiceClient {
	return &frameServiceClient{cc}
}

func (c *frameServiceClient) Frames(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[FramesRequest, FrameEvent], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &FrameService_ServiceDesc.Streams[0], FrameService_Frames_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[FramesRequest, FrameEvent]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type FrameService_FramesClient = grpc.BidiStreamingClient[FramesRequest, FrameEvent]

// FrameServiceServer is the server API for FrameService service.
// All implementations must embed UnimplementedFrameServiceServer
// for forward compatibility.
//
// FrameService is the second service on the control socket, carrying handles and not pixels.
//
// A decoded frame stays in the GPU memory the decoder allocated.
// What crosses here is the name of that memory, a shared texture handle on Windows or a dmabuf
// file descriptor on Linux, with the bookkeeping that says whose turn it is to touch it.
// Pixels never enter a message, never reach system memory and never reach ControlService.
//
// Nothing here describes a tile or a grid.
// A subscription names a picture some effect already opened, and the size it asks to be
// rendered at is a count of pixels rather than a layout
// (docs/ipc-api.md, docs/viewer-architecture.md).
type FrameServiceServer interface {
	// Frames subscribes one consumer to one decode.
	//
	// Bidirectional because the protocol is a loan: the backend lends a slot of shared memory
	// and writes into it again only once the consumer hands it back, so the release rides the
	// call the frame did.
	// A release on a second call could outlive the subscription it belongs to.
	//
	// One call per decode rather than one call for all of them, so a tile that ends cancels
	// its own subscription and a consumer that dies mid-frame is one dead stream rather than
	// every stream stalling on a slot nobody returns.
	//
	// FAILED_PRECONDITION where nothing is producing what the subscription named.
	// This service opens none of the three pictures: StartReceive opens a relay decode, the
	// publish opens its own preview, and StartMonitorPreview opens a monitor's.
	Frames(grpc.BidiStreamingServer[FramesRequest, FrameEvent]) error
	mustEmbedUnimplementedFrameServiceServer()
}

// UnimplementedFrameServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedFrameServiceServer struct{}

func (UnimplementedFrameServiceServer) Frames(grpc.BidiStreamingServer[FramesRequest, FrameEvent]) error {
	return status.Error(codes.Unimplemented, "method Frames not implemented")
}
func (UnimplementedFrameServiceServer) mustEmbedUnimplementedFrameServiceServer() {}
func (UnimplementedFrameServiceServer) testEmbeddedByValue()                      {}

// UnsafeFrameServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to FrameServiceServer will
// result in compilation errors.
type UnsafeFrameServiceServer interface {
	mustEmbedUnimplementedFrameServiceServer()
}

func RegisterFrameServiceServer(s grpc.ServiceRegistrar, srv FrameServiceServer) {
	// If the following call panics, it indicates UnimplementedFrameServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&FrameService_ServiceDesc, srv)
}

func _FrameService_Frames_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(FrameServiceServer).Frames(&grpc.GenericServerStream[FramesRequest, FrameEvent]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type FrameService_FramesServer = grpc.BidiStreamingServer[FramesRequest, FrameEvent]

// FrameService_ServiceDesc is the grpc.ServiceDesc for FrameService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var FrameService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "screenshare.v1.FrameService",
	HandlerType: (*FrameServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Frames",
			Handler:       _FrameService_Frames_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "screenshare/v1/frame.proto",
}
