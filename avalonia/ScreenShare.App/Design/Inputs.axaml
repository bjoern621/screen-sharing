<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!--
    The controls a person types into or drags, keyed on the type so a bare TextBox or Slider gets the design.
    A field that has to name a theme is a field that can forget to.
    Fluent's template is replaced rather than dressed, for the reason Design/Buttons.axaml gives.
    No CheckBox theme: a flag is the switch in Controls/Toggle, so the product holds no CheckBox.
  -->

  <!-- Text  -->

  <!--
    Tabular figures: a bitrate or a port is read digit by digit,
    and proportional numerals move the text under the caret as it is typed.
    The placeholder is prose about what belongs in the box rather than a dimmed value,
    so it takes the hint colour and the proportional numerals prose is set in.
  -->
  <ControlTheme x:Key="{x:Type TextBox}" TargetType="TextBox">
    <Setter Property="Background" Value="{StaticResource ControlBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource RadiusControl}" />
    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
    <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
    <Setter Property="FontFeatures" Value="{StaticResource FigureFeatures}" />
    <Setter Property="MinHeight" Value="{StaticResource ControlHeight}" />
    <Setter Property="Padding" Value="10,0" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="CaretBrush" Value="{StaticResource ForegroundBrush}" />
    <Setter Property="SelectionBrush" Value="{StaticResource SelectedBrush}" />
    <Setter Property="SelectionForegroundBrush" Value="{StaticResource OnSelectedBrush}" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Name="Root"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{TemplateBinding CornerRadius}">
          <ScrollViewer Name="PART_ScrollViewer"
                        HorizontalScrollBarVisibility="Hidden"
                        VerticalScrollBarVisibility="Hidden"
                        Padding="{TemplateBinding Padding}">
            <Panel>
              <TextBlock Name="PART_Watermark"
                         Text="{TemplateBinding PlaceholderText}"
                         FontFamily="{StaticResource UiFont}"
                         Foreground="{StaticResource HintBrush}"
                         IsVisible="{TemplateBinding Text, Converter={x:Static StringConverters.IsNullOrEmpty}}"
                         VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
              <TextPresenter Name="PART_TextPresenter"
                           Text="{TemplateBinding Text, Mode=TwoWay}"
                           CaretIndex="{TemplateBinding CaretIndex}"
                           SelectionStart="{TemplateBinding SelectionStart}"
                           SelectionEnd="{TemplateBinding SelectionEnd}"
                           CaretBrush="{TemplateBinding CaretBrush}"
                           SelectionBrush="{TemplateBinding SelectionBrush}"
                           SelectionForegroundBrush="{TemplateBinding SelectionForegroundBrush}"
                           PasswordChar="{TemplateBinding PasswordChar}"
                           RevealPassword="{TemplateBinding RevealPassword}"
                           TextAlignment="{TemplateBinding TextAlignment}"
                           TextWrapping="{TemplateBinding TextWrapping}"
                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Panel>
          </ScrollViewer>
        </Border>
      </ControlTemplate>
    </Setter>

    <Style Selector="^:pointerover /template/ Border#Root">
      <Setter Property="Background" Value="{StaticResource ControlHoverBrush}" />
    </Style>

    <!-- The edge and not a fill, since the box a caret sits in must not shift under it. -->
    <Style Selector="^:focus /template/ Border#Root">
      <Setter Property="Background" Value="{StaticResource ControlBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource ForegroundBrush}" />
    </Style>

    <Style Selector="^:disabled">
      <Setter Property="Foreground" Value="{StaticResource DisabledBrush}" />
    </Style>
    <Style Selector="^:disabled /template/ Border#Root">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}" />
    </Style>
  </ControlTheme>

  <!--
    The same box, with the stepper dropped rather than restyled.
    The design states no stepper, and minimum, maximum and increment hold whether or not arrows are drawn.
    A field whose steps are worth dragging is offered as a slider by the form instead.
  -->
  <!--
    CornerRadius stays a property the box reads rather than a value the template fixes,
    so a number glued to a control beside it states that corner and inherits the rest (Controls/NumberSelect).
  -->
  <ControlTheme x:Key="{x:Type NumericUpDown}" TargetType="NumericUpDown">
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="CornerRadius" Value="{StaticResource RadiusControl}" />
    <Setter Property="Template">
      <ControlTemplate>
        <TextBox Name="PART_TextBox"
                 Text="{TemplateBinding Text, Mode=TwoWay}"
                 IsReadOnly="{TemplateBinding IsReadOnly}"
                 CornerRadius="{TemplateBinding CornerRadius}"
                 HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                 VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
      </ControlTemplate>
    </Setter>
  </ControlTheme>

  <!-- Slider  -->

  <!-- The 3px ring is the card fill behind the track, which is what punches the thumb out of it. -->
  <ControlTheme x:Key="SliderThumb" TargetType="Thumb">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <ControlTemplate>
        <Ellipse Name="Knob"
                 Fill="{StaticResource ForegroundBrush}"
                 Stroke="{StaticResource CardBrush}"
                 StrokeThickness="3" />
      </ControlTemplate>
    </Setter>

    <Style Selector="^:disabled">
      <Setter Property="Cursor" Value="Arrow" />
    </Style>
    <Style Selector="^:disabled /template/ Ellipse#Knob">
      <Setter Property="Fill" Value="{StaticResource DisabledBrush}" />
    </Style>
  </ControlTheme>

  <ControlTheme x:Key="SliderTrackFill" TargetType="RepeatButton">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Name="Fill"
                Height="6"
                VerticalAlignment="Center"
                CornerRadius="{StaticResource RadiusSegment}"
                Background="{StaticResource ForegroundBrush}" />
      </ControlTemplate>
    </Setter>

    <Style Selector="^:disabled">
      <Setter Property="Cursor" Value="Arrow" />
    </Style>
    <Style Selector="^:disabled /template/ Border#Fill">
      <Setter Property="Background" Value="{StaticResource DisabledBrush}" />
    </Style>
  </ControlTheme>

  <!--
    A half of the track that paints nothing: ahead of the thumb on a plain slider,
    and both halves of the quantizer, whose zones the form behind it draws.
    Transparent rather than absent, since the half is still the target that pages the thumb.
  -->
  <ControlTheme x:Key="SliderTrackBlank" TargetType="RepeatButton">
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border Background="Transparent" />
      </ControlTemplate>
    </Setter>
  </ControlTheme>

  <!--
    Horizontal only.
    A vertical arm the design never states is one nobody would notice had rotted.
  -->
  <ControlTheme x:Key="{x:Type Slider}" TargetType="Slider">
    <Setter Property="Background" Value="{StaticResource DividerBrush}" />
    <Setter Property="Height" Value="14" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Template">
      <ControlTemplate>
        <Panel Background="Transparent">
          <Border Height="6"
                  VerticalAlignment="Center"
                  CornerRadius="{StaticResource RadiusSegment}"
                  Background="{TemplateBinding Background}" />
          <Track Name="PART_Track"
                 Orientation="Horizontal"
                 Minimum="{TemplateBinding Minimum}"
                 Maximum="{TemplateBinding Maximum}"
                 Value="{TemplateBinding Value, Mode=TwoWay}">
            <Track.DecreaseButton>
              <RepeatButton Name="PART_DecreaseButton" Theme="{StaticResource SliderTrackFill}" />
            </Track.DecreaseButton>
            <Track.IncreaseButton>
              <RepeatButton Name="PART_IncreaseButton" Theme="{StaticResource SliderTrackBlank}" />
            </Track.IncreaseButton>
            <Thumb Theme="{StaticResource SliderThumb}" Width="14" Height="14" />
          </Track>
        </Panel>
      </ControlTemplate>
    </Setter>
  </ControlTheme>

</ResourceDictionary>
